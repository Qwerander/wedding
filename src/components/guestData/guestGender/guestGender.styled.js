import styled from "styled-components";

export const GuestGenderStl = styled.div`
        .wrapper {
            display: flex;
            gap: 45px;
        }

        label {
            display: flex;
            gap: 6px;
        }

        input[type='radio'] {
            width: 24px;
        }
`